{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    
    <div class="container mt-4">
        <h1 class="font-weight-bold">
            Accueil
        </h1>
        {% if app.user %}
            {% if is_granted('ROLE_ADMIN_MUSEO') %}
                <a href="{{ path('app_logout') }}">Backoffice</a> 
            {% endif %}
            <span>Vous êtes connecter avec <strong>{{ app.user.username }}</strong>,  <a href="{{ path('app_logout') }}" class="btn btn-danger">Deconnexion</a> </span>
        {% else %}
            <a href="{{ path('app_login') }}">Connexion</a>
        {% endif %}
    </div>

{% endblock %}