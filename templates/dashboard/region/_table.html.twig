<div class="col-12 md-12">
    <table class="table table-striped">
        <thead>
        <tr>
            <th scope="col">#</th>
            <th>Nom</th>
            <th>Créer le</th>
            <th>Actions</th>
        </tr>
        </thead>

        <tbody>
        {% for region in regions %}
        <tr>
            <td>{{ region.id }}</td>
            <td>{{ region.name }}</td>
            <td>{{ region.createdAt|date("d/m/Y") }}</td>
            <td>
                <a href="{{ path('dashboard.region.edit', {id: region.id}) }}" class="btn btn-primary"><i class="far fa-edit"></i></a>
                <a href="{{ path('dashboard.region.delete', {id: region.id}) }}" class="btn btn-danger"><i class="far fa-trash-alt"></i></a>
            </td>
        </tr>
        {% endfor %}
        </tbody>
    </table>
</div>