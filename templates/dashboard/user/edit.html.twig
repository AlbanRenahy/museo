{% extends 'base.html.twig' %}

{% block title 'Edition Monument' %}

{% block body %}

{% include 'layout/sidebar.html.twig' %}

<div class="wrapper">
    <main class="container-fluid">
        {{ form_start(form) }}
        <h1>Edition d'un monument</h1>
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-success">Editer le monument</button>
            <a href="{{ path('dashboard.monument.index') }}" class="btn btn-info">Revenir sur la liste</a>
        {{ form_end(form) }}
    </main>
</div>

{% endblock %}