const initialState = {
  // ici l'état initial
};

const reducerModele = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducerModele;
